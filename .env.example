# Shared
DATABASE_URL="mysql://root:password@localhost:3306/cpl"
# Shared by platform + tracking for internal APIs (lead submit, CPA sale notify).
# Send as header: X-Service-Token
INTERNAL_SERVICE_TOKEN="change-me-to-a-random-64-char-secret"

# Platform (leadvix.io) — apps/platform/.env
AUTH_SECRET="your-secret-key-min-32-characters"
AUTH_URL="https://leadvix.io"
APP_URL="https://leadvix.io"
PLATFORM_URL="https://leadvix.io"
TRACKING_URL="https://leadgenlink.site"
NEXT_PUBLIC_PLATFORM_URL="https://leadvix.io"
NEXT_PUBLIC_TRACKING_URL="https://leadgenlink.site"
# Public server IP for advertiser custom-domain A records (not 127.0.0.1 from /etc/hosts)
PLATFORM_PUBLIC_IP="13.235.217.121"
PORT=3000

# Tracking (leadgenlink.site) — apps/tracking/.env
TRACKING_URL="http://leadgenlink.site"
PLATFORM_URL="http://leadvix.io"
NEXT_PUBLIC_TRACKING_URL="http://leadgenlink.site"
NEXT_PUBLIC_PLATFORM_URL="http://leadvix.io"

# Optional fraud geo lookup for smart link rotation
FRAUD_IP_API_KEY=

# Email, payments, etc. (platform only)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
REDIS_URL="redis://localhost:6379"
SENTRY_DSN=""
# Transactional email — Mailgun (preferred) or SMTP fallback
# Mailgun HTTP API (Sending key from Mailgun dashboard)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=mg.leadvix.io
MAILGUN_FROM="Leadvix <noreply@mg.leadvix.io>"
MAILGUN_API_BASE=https://api.mailgun.net
# HTTP webhook signing key (Mailgun → Sending → Webhooks). Required for delivery/bounce stats.
# Point domain webhooks (delivered, permanent_fail, complained) to:
#   https://leadvix.io/api/v1/webhooks/mailgun
MAILGUN_WEBHOOK_SIGNING_KEY=
# Mailgun SMTP (username + password from Mailgun dashboard — used if API fails or unset)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=postmaster@mg.leadvix.io
SMTP_PASS=
SMTP_FROM="Leadvix <noreply@mg.leadvix.io>"
ADMIN_ALERT_EMAIL=admin@leadvix.io
SUPPORT_EMAIL=support@leadvix.io
# Optional Mailgun-verified From for bulk support email (defaults to support@${MAILGUN_DOMAIN})
# SUPPORT_FROM_EMAIL=support@mg.leadvix.io
# EmailListVerify API key (apps.emaillistverify.com) — lead fraud email validation
FRAUD_EMAIL_API_KEY=
# Autoresponder integrations
INTEGRATION_ENCRYPTION_KEY=
# AWeber OAuth (Labs app). Register redirect:
#   http://localhost:3010/api/v1/advertiser/integrations/aweber/callback
#   https://leadvix.io/api/v1/advertiser/integrations/aweber/callback
AWEBER_CLIENT_ID=
AWEBER_CLIENT_SECRET=
# Optional override (defaults to {PLATFORM_URL}/api/v1/advertiser/integrations/aweber/callback)
AWEBER_REDIRECT_URI=
# CPA Offer Marketplace — open to all advertisers by default when unset.
# Set a comma-separated email list to restrict (e.g. advertiser@cpl.local).
# Use * or empty for all advertisers.
CPA_OFFERS_ADVERTISER_ALLOWLIST=*
# Autoresponder — open to all advertisers by default when unset.
# Set a comma-separated email list to restrict (e.g. advertiser@cpl.local).
# Use * or empty for all advertisers.
AUTORESPONDER_ADVERTISER_ALLOWLIST=*
# Marketing email uses Mailgun (same keys as transactional above).
# Delivery/bounce/complaint: MAILGUN_WEBHOOK_SIGNING_KEY + Mailgun domain webhooks → /api/v1/webhooks/mailgun
